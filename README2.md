# Happy Tails Pet Cafe UAT Questions

These UAT items are based on the current code, UI routes, Supabase schema, services, and README manual in this repository.

| # | UAT Question | User Role | Feature/Module | Expected Result | Pass/Fail Criteria | Evidence |
|---:|---|---|---|---|---|---|
| 1 | Can a visitor open the home page and see the storefront sections? | Guest/Customer | Home | Home loads hero, announcements, menu of the day, menu belt, about teaser, and reviews. | Pass if `/` renders without login and sections appear; fail if route crashes. | Page `/`; `src/pages/Home.jsx`; `src/components/HeroSlider.jsx`; `src/components/CampaignBanner.jsx`; `src/components/CommunityReviews.jsx` |
| 2 | Does the home page show only the latest 5 public reviews? | Guest/Customer | Home Reviews | Community Reviews renders at most 5 public reviews. | Pass if only 5 latest review cards appear; fail if 6+ appear. | `src/services/reviewService.js` uses `reviews.slice(0, 5)`; `src/components/CommunityReviews.jsx`; DB `customer_order_reviews.is_public` |
| 3 | Can owner announcements appear on the home banner? | Owner/Customer | Announcements | Active announcement text saved by owner appears in the moving banner. | Pass if active saved text appears on `/`; fail if saved active text is missing after refresh. | `/owner/settings`; `src/staff/pages/SettingsPage.tsx`; `src/staff/services/campaignAnnouncementService.ts`; `src/components/CampaignBanner.jsx`; DB `campaign_announcements`, `business_settings.campaign_announcements` |
| 4 | Can customers browse the cafe menu? | Guest/Customer | Menu | Menu page loads categories/items with prices and images. | Pass if `/menu` shows menu content; fail if items are blank when DB has active items. | `/menu`; `src/pages/Menu.jsx`; `src/services/menuService.js`; DB `menu_categories`, `menu_items` |
| 5 | Can customers open an order category and add menu items? | Guest/Customer | Ordering | Category page shows filtered items and add-to-cart controls. | Pass if item can be added to cart; fail if valid available item cannot be added. | `/order`; `/order/:category`; `src/pages/Order.jsx`; `src/pages/OrderCategory.jsx`; `src/context/CartContext.jsx` |
| 6 | Can a guest place a normal order without creating an account? | Guest | Guest Checkout | Guest checkout saves identity locally and submits through the order RPC. | Pass if guest order reaches success page; fail if login is required for non-loyalty checkout. | `/checkout`; `src/pages/Checkout.jsx`; `src/services/orderService.js`; `src/services/guestIdentity.js`; DB RPC `create_customer_order` |
| 7 | Are loyalty reward items blocked for guests? | Guest | Loyalty Checkout | Guest cannot claim loyalty reward items. | Pass if checkout shows sign-in/regular item requirement; fail if guest submits reward order. | `src/services/orderService.js`; `src/pages/Checkout.jsx`; DB RPC `create_customer_order` |
| 8 | Does checkout enforce owner-enabled order types? | Customer/Guest | Checkout Settings | Only enabled order types are selectable. | Pass if disabled order types are hidden/blocked; fail if disabled type can be submitted. | `/owner/settings`; `/checkout`; `src/services/businessSettingsService.js`; `src/pages/Checkout.jsx`; DB `business_settings` |
| 9 | Does checkout enforce owner-enabled payment methods? | Customer/Guest | Payments | Only enabled payment methods are selectable. | Pass if disabled methods are hidden/blocked; fail if disabled payment method submits. | `/owner/settings`; `/checkout`; `src/pages/Checkout.jsx`; `src/utils/paymentMethods.js`; DB `business_settings` |
| 10 | Does online payment require receipt proof? | Customer/Guest | Payments | Non-cash payments require receipt upload; cash does not. | Pass if non-cash without receipt is blocked and cash proceeds; fail otherwise. | `src/pages/Checkout.jsx`; DB constraint `orders_receipt_required_for_non_cash`; RPC `create_customer_order` |
| 11 | Does delivery checkout require a valid covered address? | Customer/Guest | Delivery | Delivery order requires active coverage, purok/address, map pin, and delivery fee. | Pass if incomplete/out-of-coverage delivery is blocked; fail if it submits. | `/checkout`; `/owner/admin/delivery-coverage`; `src/components/DeliveryAddressForm.jsx`; `src/services/deliveryAreaService.js`; `supabase/delivery_area_schema.sql` |
| 12 | Does order success provide guest-accessible follow-up links? | Customer/Guest | Order Success | After order success, user can track, view history, or order again. | Pass if buttons render on `/order-success`; fail if no navigation appears. | `/order-success`; `src/pages/OrderSuccess.jsx` |
| 13 | Can guests track their latest order from the same browser? | Guest | Track Order | Guest tracking reads saved guest identity/last order and calls guest tracking RPC. | Pass if latest guest order appears; fail if same-browser guest order cannot be found. | `/track-order`; `src/hooks/useOrderTracking.js`; `src/services/orderService.js`; `src/services/guestIdentity.js`; DB RPC `get_guest_order_for_tracking` |
| 14 | Can guests view order history/details without logging in? | Guest | Order History | Guest order history shows saved guest order and manual order lookup. | Pass if `/order-history` opens without login and lookup works; fail if redirected to login. | `/order-history`; `src/App.jsx`; `src/pages/OrderHistory.jsx`; `src/services/orderService.js` |
| 15 | Is customer order history paginated to 5 orders per page? | Customer | Order History | History page loads 5 orders per page. | Pass if only 5 orders appear per page with pagination; fail if page shows more. | `src/pages/OrderHistory.jsx` constant `ORDERS_PER_PAGE = 5`; `src/services/orderService.js` |
| 16 | Can customers open order details from order history? | Customer/Guest | Order History | Expanding a history card shows items and totals. | Pass if View details opens item/totals panel; fail if card cannot expand. | `/order-history`; `src/pages/OrderHistory.jsx`; `src/pages/OrderHistory.css` |
| 17 | Does order tracking show status timeline and cancellation rules? | Customer/Guest | Track Order | Tracking shows status, timeline, cancellation button only when pending. | Pass if pending can cancel and later statuses cannot; fail if invalid cancellation is allowed. | `/track-order`; `src/pages/TrackOrder.jsx`; `src/services/orderService.js`; DB `order_status_history` |
| 18 | Can customers submit a review after completed/delivered orders? | Customer/Guest | Reviews | Review prompt appears for eligible completed/delivered orders. | Pass if completed/delivered order allows one review; fail if pending orders can review. | `src/components/ReviewPrompt.jsx`; `src/services/reviewService.js`; DB RPC `submit_customer_order_review` |
| 19 | Can public reviews be loaded on the customer home page? | Guest/Customer | Reviews | Public reviews from DB appear on Community Reviews. | Pass if `is_public=true` reviews render; fail if public reviews are absent despite data. | `/`; `src/components/CommunityReviews.jsx`; `src/services/reviewService.js`; DB `customer_order_reviews` |
| 20 | Can customers view profile information when logged in? | Customer | Profile | Profile info page loads account/customer details. | Pass if `/profile/info` is protected and displays profile fields; fail if guest opens it. | `/profile/info`; `src/App.jsx`; `src/pages/Profile.jsx`; `src/auth/RequireRole.jsx`; DB `profiles` |
| 21 | Can customers view loyalty rewards? | Customer | Loyalty | Loyalty page displays stamp/reward data. | Pass if logged-in customer sees loyalty data; fail if page crashes. | `/profile/loyalty`; `src/pages/Profile.jsx`; `src/services/loyaltyService.js`; DB `loyalty_accounts`, `loyalty_rewards` |
| 22 | Can staff access operational dashboard but not owner-only pages? | Staff | Role Access | Staff can open `/staff/dashboard`; owner pages redirect/block. | Pass if staff cannot open `/staff/reviews`, `/staff/riders`, `/staff/settings`; fail if staff can manage them. | `src/App.jsx`; `src/auth/RequireRole.jsx`; `src/staff/components/dashboard/DashboardLayout.tsx` |
| 23 | Can owners access owner-only reviews and rider tabs? | Owner | Owner Navigation | Owner sidebar and quick search include Reviews and Rider Management. | Pass if owner sees links and pages load; fail if staff sees/manages them. | `/owner/reviews`; `/owner/riders`; `src/App.jsx`; `src/staff/components/dashboard/DashboardLayout.tsx`; `src/staff/components/navigation/CommandBar.tsx` |
| 24 | Can staff/owner search and filter orders? | Staff/Owner | Orders | Orders page supports search, status, and date range filters. | Pass if filters change order list; fail if controls do nothing. | `/staff/orders`; `/owner/orders`; `src/staff/pages/orders/OrdersPage.tsx`; `src/staff/hooks/useOrders.ts`; `src/staff/services/orderService.ts` |
| 25 | Can staff/owner confirm payment? | Staff/Owner | Orders/Payments | Confirm Payment updates order payment status to paid. | Pass if paid status persists; fail if button does not update DB. | `src/staff/pages/orders/OrdersPage.tsx`; `src/staff/services/orderService.ts`; DB `orders.payment_status` |
| 26 | Can staff/owner update order status and timeline? | Staff/Owner | Orders | Status update changes order status and inserts status history. | Pass if order status and timeline update; fail if one is missing. | `src/staff/pages/orders/OrdersPage.tsx`; `src/staff/services/orderService.ts`; DB `orders.status`, `order_status_history` |
| 27 | Can owner assign a rider to a delivery order? | Owner | Rider Assignment | Delivery order details show rider selector and Assign Rider action. | Pass if selected active rider is saved to order delivery details; fail if non-delivery orders show assignment. | `/owner/orders`; `src/staff/pages/orders/OrdersPage.tsx`; DB `delivery_riders`, `orders.rider_id`, `orders.delivery_address.riderAssignment` |
| 28 | Do customer tracking/history show assigned rider details? | Customer/Guest | Rider Assignment | Delivery order shows assigned rider name/contact/vehicle after owner assignment. | Pass if rider appears in `/track-order` and `/order-history`; fail if saved assignment is hidden. | `src/pages/TrackOrder.jsx`; `src/pages/OrderHistory.jsx`; `src/services/orderService.js` |
| 29 | Can owner add, edit, deactivate, and reactivate riders? | Owner | Rider Management | Rider Management supports rider CRUD-style active state management. | Pass if rider records save and status toggles; fail if form cannot persist. | `/owner/riders`; `src/staff/pages/owner/RiderManagementPage.tsx`; DB `delivery_riders`; `supabase/unified_schema.sql` |
| 30 | Can owner review customer feedback and hide/publish reviews? | Owner | Review Management | Owner Reviews shows stats, filters, details, and public visibility toggle. | Pass if review visibility changes `is_public`; fail if toggle does not persist. | `/owner/reviews`; `src/staff/pages/owner/ReviewsPage.tsx`; DB `customer_order_reviews.is_public` |
| 31 | Can staff/owner manage Daily Menu? | Staff/Owner | Daily Menu | Daily menu page publishes selected items for customer home page. | Pass if published daily menu appears on home page; fail if saved data is not shown. | `/staff/daily-menu`; `/owner/daily-menu`; `src/staff/pages/menu/DailyMenuPage.tsx`; `src/components/dailyMenu/MenuOfTheDay.jsx`; DB `daily_menus`, `daily_menu_items` |
| 32 | Can staff/owner manage menu items and categories? | Staff/Owner | Menu Management | Menu Management edits item/category details, images, availability, and discounts. | Pass if saved changes appear on customer menu/order pages; fail if DB update does not reflect. | `/staff/menu`; `src/staff/pages/menu/MenuManagementPage.tsx`; `src/staff/services/menuService.ts`; DB `menu_items`, `menu_categories` |
| 33 | Can staff/owner manage inventory? | Staff/Owner | Inventory | Inventory page displays stock and records movements/corrections. | Pass if movement changes stock and appears in log; fail if stock does not update. | `/staff/inventory`; `src/staff/pages/menu/InventoryManagementPage.tsx`; `src/staff/pages/menu/InventoryTrackerSection.tsx`; DB `inventory_items`, `inventory_stock_movements` |
| 34 | Can staff view customer loyalty and reset cards? | Staff/Owner | Customer Loyalty | Customer Loyalty page lists customers and supports audited reset. | Pass if reset updates loyalty and logs activity; fail if reset is unavailable to staff/owner. | `/staff/customers`; `src/staff/pages/customers/CustomersLoyaltyPage.tsx`; `src/staff/services/loyaltyService.ts`; DB RPC `reset_customer_loyalty_card` |
| 35 | Can owner save business settings and checkout settings? | Owner | Settings | Settings page saves business details, order type/payment toggles, and cutoff schedule. | Pass if saved values affect customer checkout/home; fail if values do not persist. | `/owner/settings`; `src/staff/pages/SettingsPage.tsx`; `src/staff/services/businessSettingsService.ts`; DB `business_settings` |
| 36 | Can owner manage staff access? | Owner | Staff Access | Owner can grant/revoke staff role by email. | Pass if staff account gains/loses `/staff` access; fail if role is unchanged. | `/owner/settings`; `src/staff/pages/SettingsPage.tsx`; `src/staff/services/staffService.ts`; DB `profiles.role` |
| 37 | Can owner manage delivery coverage? | Owner | Delivery Coverage | Owner can configure active delivery area, puroks, pickup point, and radius. | Pass if coverage affects checkout validation; fail if delivery ignores settings. | `/owner/admin/delivery-coverage`; `src/staff/pages/admin/DeliveryCoveragePage.tsx`; `src/staff/services/deliveryCoverageService.ts`; DB `delivery_areas`, `delivery_puroks` |
| 38 | Can owner import sales data and inspect activity logs? | Owner | Admin Tools | Owner can access imports and activity log pages. | Pass if owner pages load and staff is blocked; fail if pages are available to staff. | `/owner/imports`; `/owner/admin/activity-log`; `src/staff/pages/imports/ImportsReportsPage.tsx`; `src/staff/pages/admin/ActivityLogPage.tsx`; DB `sales_import_batches`, `activity_logs` |
